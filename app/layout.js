export const metadata = { title: "AI Chat" };
export default function RootLayout({ children }) {
  return (<html lang="id"><body style={{background:'#18181b',color:'white',margin:0,fontFamily:'sans-serif'}}>{children}</body></html>);
}
